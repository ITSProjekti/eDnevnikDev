@model IEnumerable<eDnevnikDev.Models.Napomena>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Cas.Opis)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Profesor.Ime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Ucenik.ImeOca)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Opis)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Cas.Opis)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Profesor.Ime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ucenik.ImeOca)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Opis)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.NapomenaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.NapomenaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.NapomenaId })
        </td>
    </tr>
}

</table>
